//Temperature Conversion: Write an `if-else` statement to check if a temperature in Celsius is above 100
//degrees. Print "Above boiling point" if true, otherwise "Below boiling point."

let temp = 101;

if (temp > 100) {
  console.log("Above boiling point");
} else {
  console.log("Below boling point");
}
