//String Length Even: Write an `if-else` statement to check if the length of a string is an even number.
//Print "Even length" if true, otherwise "Odd length."

let text = "laptop";

if (text.length % 2 == 0) {
  console.log("Even length");
} else {
  console.log("Odd length");
}
