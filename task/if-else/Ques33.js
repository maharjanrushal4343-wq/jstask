// Password Length Check: Write an `if-else` statement to check if a password is at least 8 characters
//long. Print "Valid password" if true, otherwise "Password too short."

let pass = "passwor";

if (pass.length >= 8) {
  console.log("Valid Password");
} else {
  console.log("Password too short");
}
