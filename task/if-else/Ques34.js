//Check for Zero: Write an `if-else` statement to check if a number is zero. Print "Zero" if true,
//otherwise "Not zero."

let num = 0;

if (num === 0) {
  console.log("Zero");
} else {
  console.log("Non-zero");
}