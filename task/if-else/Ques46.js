//Check for Digit: Write an `if-else` statement to check if a character is a digit (0-9). Print "Digit" if
//true, otherwise "Not a digit."

let num = 1;

if (num >= 0 && num <= 9) {
  console.log("Digit");
} else {
  console.log("Not a digit");
}
